import React from 'react'
import Base from '../core/Base'

const UserDashboard = () => {
    return (
        // Base also takes description
        <Base title = "User Dashboard">
            <h1>Welcome to user dashboard </h1>
        </Base>
    )
}

export default UserDashboard
